<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="300dp"
    android:layout_height="120dp"
    android:layout_centerHorizontal="true"
    android:layout_alignParentBottom="true">

    <!--wheelview color B2464646-->
    <com.lukedeighton.wheelview.WheelView
        android:id="@+id/wheelview"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:wheelColor="@null"
        app:rotatableWheelDrawable="false"
        app:selectionAngle="90.0"
        app:wheelPosition="bottom"
        app:wheelOffsetY="60dp"
        app:repeatItems="true"
        app:wheelRadius="170dp"
        app:wheelItemCount="10"
        app:wheelPadding="13dp"
        app:wheelItemRadius="27dp"/>
    <!--
            <com.lukedeighton.wheelview.WheelView
                android:id="@+id/wheelview"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                app:wheelColor="#B2464646"
                app:rotatableWheelDrawable="false"
                app:selectionAngle="90.0"
                app:wheelPosition="bottom"
                app:wheelOffsetY="60dp"
                app:repeatItems="true"
                app:wheelRadius="150dp"
                app:wheelItemCount="10"
                app:wheelPadding="13dp"
                app:wheelItemRadius="20dp"/>-->
    <TextView
        android:id="@+id/item_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:paddingLeft="7dp"
        android:paddingRight="7dp"
        android:layout_marginTop="5dp"
        android:textSize="15dp"
        android:text="@string/wheel_view_item1"
        android:layout_centerHorizontal="true"
        android:background="@drawable/wheel_txt_shape"
        android:textColor="#ff00d7f1"/>
</RelativeLayout>
